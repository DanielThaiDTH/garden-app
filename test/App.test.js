import App from "../App";
import renderer from 'react-test-renderer';

test('App test', ()=> {
    // let renderedApp;
    // renderer.act(() => {
    //     renderedApp = renderer.create(<App/>).toJSON();
    // });
    // console.log(renderedApp);
});